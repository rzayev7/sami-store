# Copy to `.env` — never commit real secrets.

MONGODB_URI=
JWT_SECRET=

# Optional: extra allowed CORS origin (see server.js)
# FRONTEND_URL=

# Order emails — contact line in templates (defaults to samistore.support@gmail.com if unset)
# SUPPORT_EMAIL=samistore.support@gmail.com
